# Proposed improvements (advisory — AI flags, human decides)

_Generated 2026-06-15 05:03 UTC · model: claude-sonnet-4-6 · source: `reports/agent_eval (latest 8 report(s))` · via `evals/improve.py`._

> **Advisory only.** Nothing here is applied automatically. A human reviews and, if approved, edits the skill library by hand (through the normal pre-commit + eval-validation gate). This analyst has **no write access** to code, guards, or the eval answer key.

---

Now I have everything I need. Here is the advisory report.

---

# Advisory Report — Agent-Behavior Eval History · Compliance Skill Library
*Prepared 2026-06-15 · Read-only analysis · Human applies any approved change.*

---

## 1. Summary

| # | Finding | Type | Real issue or eval artifact? | Recurring? | Proposed change (file) |
|---|---|---|---|---|---|
| 1 | Systematic 0/3 (tool selection + facts) across all 5 prompts simultaneously in 2 sessions | Automation flag | Environment/infrastructure failure — not a skill library issue | Yes — 2 sessions (2026-06-14 07:24, 2026-06-15 04:19) | None — flag for human |
| 2 | A1 (`assembling-watchlist-triage`) §4.2 intermittent: 1-of-3 runs failed §4.2 in 2 separate sessions | Proactive | Real behavior problem: model wrote a self-authored compliance disclaimer containing credit vocabulary, despite skill instruction not to. Partially a use-vs-mention artifact. | Yes — sessions 2026-06-14 03:55 and 2026-06-13 02:24 | Add concrete forbidden-phrase examples to `assembling-watchlist-triage/SKILL.md` |
| 3 | A3 (`measuring-engagement-coverage`) §4.2 intermittent: 1-of-3 runs in 2 sessions | Proactive | Same root cause as Finding 2 | Yes — sessions 2026-06-14 17:13 and 2026-06-14 03:55 | Add concrete forbidden-phrase examples to `measuring-engagement-coverage/SKILL.md` |
| 4 | B3 (`detecting-cross-source-discrepancies`) 1/3 pass, tool selection 2/3, facts 1/3 in one session | — | Likely transient (co-occurred with Finding 1 environment anomaly in the adjacent session; no recurrence) | No — single session | No change proposed |

---

## 2. Must-fix

None — no failed or recurring runs attributable to a skill library deficiency. The two recurring systematic 0/3 sessions (Finding 1) are environment failures not addressable through skill text. The recurring §4.2 hits on A1 and A3 (Findings 2–3) are classified as proactive because the skill instructions are already substantively correct, the latest run is clean, and the proposed changes are additive hardening rather than fixes to a compliance gap.

---

## 3. Proactive (optional)

### Finding 2 — A1 `assembling-watchlist-triage` §4.2 intermittent

**Observed pattern.** In 2 of 8 sessions (2026-06-14 03:55, 2026-06-13 02:24), one of the three A1 runs produced a §4.2 hit. The latest session is clean.

**Root cause.** The skill's "do not write your own reliability footer" instruction exists and names the mechanism ("credit vocab even in negation trips the §4.2 scanner"), but it gives no concrete example of a forbidden closing phrase. Without an example, the model occasionally ends its readout with a self-authored note such as *"This is not a creditworthiness assessment"* or *"no credit-risk conclusions are drawn"* — which contain the blocked terms and fail `screen_and_finalize` deterministically. The `screening-and-gating-output` skill (which A1 cross-references) has explicit forbidden-phrase examples; A1 does not repeat them inline.

**Evaluation classification.** Partly a use-vs-mention artifact (the eval documentation flags this pattern as possible for A1/A3), but also a real behavior problem: the skill explicitly forbids self-authored disclaimers and the model still writes them intermittently. Both the skill non-compliance and the scanner hit are real; the model is not quoting a source, it is volunteering a closing disclaimer.

**Proposed skill change** — `centerline-plugin/skills/assembling-watchlist-triage/SKILL.md`

Current text (lines 25–26):

> …never append a "Reliability: High/Moderate" *scale* or a "no creditworthiness…" line (naming credit vocab **even in negation** trips the §4.2 scanner). Pass any uncertain/missing inputs…

Proposed — insert one sentence between "trips the §4.2 scanner)" and "Pass any uncertain/missing inputs":

> For example, do **not** close with: *"This is not a creditworthiness assessment,"* *"no credit-risk conclusions are drawn,"* or *"I am not characterizing credit quality"* — even in a disclaimer, these terms still appear as credit-characterization language to the scanner.

**Compliance rationale (§4.2).** The `screen_and_finalize` tool enforces §4.2 deterministically. The proposed addition provides concrete, actionable examples of the forbidden pattern — the same level of specificity already present in `screening-and-gating-output` — without introducing any new rule or softening any existing one.

**Why this does not weaken any guard or test.** The change is purely additive (example phrases, not new permissions). It does not alter `screen_and_finalize` logic, the answer-key definitions, or any guard in `guards.py` / `core.py`.

---

### Finding 3 — A3 `measuring-engagement-coverage` §4.2 intermittent

**Observed pattern.** In 2 of 8 sessions (2026-06-14 17:13, 2026-06-14 03:55), one of the three A3 runs produced a §4.2 hit.

**Root cause.** Same as Finding 2. The A3 skill is slightly more specific ("no '…not a creditworthiness judgment' line") but still stops short of a multi-phrase example. The same intermittent pattern results.

**Evaluation classification.** Same as Finding 2 — real behavior issue, partially use-vs-mention artifact.

**Proposed skill change** — `centerline-plugin/skills/measuring-engagement-coverage/SKILL.md`

Current text (lines 22–24):

> …do not write your own reliability footer or compliance disclaimer** (no "Reliability: High/Moderate" scale, no "…not a creditworthiness judgment" line — credit vocab even in negation trips §4.2). Pass the log-completeness caveat…

Proposed — insert one sentence between "credit vocab even in negation trips §4.2)" and "Pass the log-completeness caveat":

> Do not close with phrases such as *"this analysis does not assess creditworthiness,"* *"no credit-risk conclusions are drawn,"* or *"I am not characterizing credit quality"* — even as a disclaimer, these terms trip the scanner.

**Compliance rationale (§4.2) and non-weakening justification.** Identical to Finding 2: additive only, no guard or test is touched.

---

## 4. Automation / eval-loop observations (for human attention — flag only, no workflow edits proposed)

**4.1 — Recurring systematic 0/3 sessions indicate an infrastructure health-check gap.**

Two sessions failed with tool-selection = 0 and facts = 0 across all five prompts simultaneously, while §4.2 passed 3/3 in every prompt: 2026-06-14 07:24 and 2026-06-15 04:19. A skill-library failure cannot produce this pattern — no model behavior failure removes tools from all five prompts at once while leaving §4.2 clean. The most probable cause is the MCP server being unavailable (or unreachable) when the eval harness started. The immediately following run (2026-06-15 04:58) passed cleanly, confirming the failure was transient.

The concern is that, as the eval report stands, a transient MCP outage scores as 0/3 on tool selection and facts — visually indistinguishable from a systematic model compliance regression in the summary table. An MCP-server reachability check at harness startup, with a dedicated "server unavailable" error state distinct from a graded "0 passes," would prevent infrastructure noise from masking or mimicking real model-behavior failures in the run history.

**4.2 — Back-to-back runs on 2026-06-15 (04:19 and 04:58).**

Two runs started 39 minutes apart on what appears to be the same nightly cron window. The 04:19 run failed completely (Finding 4.1); the 04:58 run passed. This suggests an automatic retry fired after the failure — which is healthy behavior if intentional. Worth confirming the retry is expected rather than a duplicate cron trigger, to avoid inflating the run count on nights with outages.

**4.3 — B1/B3 have a shorter eval baseline.**

B1 and B3 appear absent from the 2026-06-13 sessions, meaning they have approximately 5 sessions of history vs. 7+ for A1–A3. The one B3 anomaly (2026-06-14 03:55, 1/3 passes) co-occurred with A1/A3 §4.2 hits and preceded the first confirmed infrastructure-failure session by a few hours — making an environment contribution plausible. It is not yet possible to separate a genuine B3 fragility from startup noise with this sample size. A few additional clean sessions would establish a reliable baseline.

**4.4 — §4.2 scanner use-vs-mention limitation.**

The eval documentation explicitly notes the §4.2 scanner can flag a blocked phrase when the model is quoting a human's words or negating the term (use-vs-mention). The scanner currently returns a binary hit. For A1 and A3, where the recurring hit pattern is most plausibly a model-written disclaimer (real violation), versus B-track prompts where the model must quote credit officer language verbatim (scribe pattern, potential false positive), a context-aware scanner mode — or a distinction between "hit in model's own narration" vs "hit inside a quoted attribution block" — would sharpen signal quality. This is a design question for the eval authors; no change is proposed here.
