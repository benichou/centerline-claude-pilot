"""centerline_mcp — local MCP server over data/ for the Centerline Claude pilot.

Compliance guards (§2.1 strip, §5 gate, guarantor-doc refusal) are enforced here,
server-side — the universal chokepoint every caller passes through.
"""
